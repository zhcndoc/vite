import{_ as a,o as r,i as t,R as o}from"./chunks/framework.Caqowgrb.js";const f=JSON.parse('{"title":"从 v3 迁移","description":"","frontmatter":{},"headers":[],"relativePath":"guide/migration-from-v3.md","filePath":"guide/migration-from-v3.md"}'),i={name:"guide/migration-from-v3.md"};function l(d,e,n,s,c,h){return r(),t("div",null,[...e[0]||(e[0]=[o('<h1 id="migration-from-v3" tabindex="-1">从 v3 迁移 <a class="header-anchor" href="#migration-from-v3" aria-label="Permalink to “从 v3 迁移”">​</a></h1><h2 id="rollup-3" tabindex="-1">Rollup 3 <a class="header-anchor" href="#rollup-3" aria-label="Permalink to “Rollup 3”">​</a></h2><p>Vite 现在正式启用 <a href="https://github.com/vitejs/vite/issues/9870" target="_blank" rel="noreferrer">Rollup 3</a>，这使得我们可以简化 Vite 内部的资源处理并同时拥有许多改进。详情请查看 <a href="https://github.com/rollup/rollup/releases/tag/v3.0.0" target="_blank" rel="noreferrer">Rollup 3 版本记录</a>。</p><p>Rollup 3 尽最大可能兼容了 Rollup 2。如果你在项目中使用了自定义的 <a href="./../config/build-options#rollup-options"><code>rollupOptions</code></a> 并（升级后）遇到了问题，请先查看 <a href="https://cn.rollupjs.org/migration/" target="_blank" rel="noreferrer">Rollup 迁移指南</a> 来更新升级你的配置。</p><h2 id="modern-browser-baseline-change" tabindex="-1">现代浏览器基准线变化 <a class="header-anchor" href="#modern-browser-baseline-change" aria-label="Permalink to “现代浏览器基准线变化”">​</a></h2><p>当前对于现代浏览器的构建目标及现调整为了默认 <code>safari14</code> 以求更广的 ES2020 兼容性（从 <code>safari13</code> 升级）。这意味着现代化构建现在可以使用 <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/BigInt" target="_blank" rel="noreferrer"><code>BigInt</code></a>，同时 <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Nullish_coalescing" target="_blank" rel="noreferrer">空值合并运算符</a> 将不再被转译。如果你需要支持更旧版本的浏览器，你可以照常添加 <a href="https://github.com/vitejs/vite/tree/main/packages/plugin-legacy" target="_blank" rel="noreferrer"><code>@vitejs/plugin-legacy</code></a>。</p><h2 id="general-changes" tabindex="-1">其他一般性变化 <a class="header-anchor" href="#general-changes" aria-label="Permalink to “其他一般性变化”">​</a></h2><h3 id="encoding" tabindex="-1">编码 <a class="header-anchor" href="#encoding" aria-label="Permalink to “编码”">​</a></h3><p>构建的默认字符集现在是 utf8（查看 <a href="https://github.com/vitejs/vite/issues/10753" target="_blank" rel="noreferrer">#10753</a> 了解更多细节）。</p><h3 id="importing-css-as-a-string" tabindex="-1">以字符串形式导入 CSS <a class="header-anchor" href="#importing-css-as-a-string" aria-label="Permalink to “以字符串形式导入 CSS”">​</a></h3><p>在过往的 Vite 3 之中，以默认导入形式导入一个 <code>.css</code> 文件的可能会造成对 CSS 的双重加载。</p><p>In Vite 4, the manifest files (<code>build.manifest</code>, <code>build.ssrManifest</code>) was generated in the root of <code>build.outDir</code> by default. From Vite 5, those will be generated in the <code>.vite</code> directory in the <code>build.outDir</code> by default.</p><p>这种双重加载出现的原因是 <code>.css</code> 文件是将会被释放（emit）到最终产物的，并且很可能 CSS 字符串将会在应用代码中被使用到，就比如被框架运行时注入的时候。对于现在的 Vite 4，<code>.css</code> 默认导出 <a href="https://github.com/vitejs/vite/issues/11094" target="_blank" rel="noreferrer">已经被废弃</a>。在这种情况下你将需要使用 <code>?inline</code> 这个查询参数后缀，而这时将不会将导入的 <code>.css</code> 样式文件释放到最终产物。</p><p>CLI shortcuts, like <code>r</code> to restart the dev server, now require an additional <code>Enter</code> press to trigger the shortcut. For example, <code>r + Enter</code> to restart the dev server.</p><h3 id="production-builds-by-default" tabindex="-1">默认情况下的生产构建 <a class="header-anchor" href="#production-builds-by-default" aria-label="Permalink to “默认情况下的生产构建”">​</a></h3><p>不管所传递的 <code>--mode</code> 是什么，<code>vite build</code> 总是构建生产版本。之前，若将 <code>mode</code> 改为 <code>production</code> 之外的模式会构建开发版本，如果现在希望用于开发构建，可以在 <code>.env.{mode}</code> 文件中设置 <code>NODE_ENV=development</code>。</p><p>在本次变动中，如果 <code>process.env.NODE_ENV</code> 已经被定义，<code>vite dev</code> 和 <code>vite build</code> 将不再覆盖它。所以如果在构建前设置了 <code>process.env.NODE_ENV = &#39;development&#39;</code>，将会构建开发版本。这在并行执行多个构建或开发服务器时提供了更多的控制权。</p><p>请参阅更新后的 <a href="/guide/env-and-mode#modes"><code>mode</code> 文档</a> 了解更多详细信息。</p><h3 id="environment-variables" tabindex="-1">环境变量 <a class="header-anchor" href="#environment-variables" aria-label="Permalink to “环境变量”">​</a></h3><p>Vite 现在使用 <code>dotenv</code> 16 和 <code>dotenv-expand</code> 9（之前是 <code>dotenv</code> 14 和 <code>dotenv-expand</code> 5）如果你有一个包含 <code>#</code> 或者 <code>`</code> 的值，你将需要将它们以双引号包裹起来。</p><div class="language-diff"><button title="Copy Code" class="copy"></button><span class="lang">diff</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#B31D28;--shiki-dark:#FDAEB7;">-VITE_APP=ab#cd`ef</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">+VITE_APP=&quot;ab#cd`ef&quot;</span></span></code></pre></div><p>了解更多详情，请查看 <a href="https://github.com/motdotla/dotenv/blob/master/CHANGELOG.md" target="_blank" rel="noreferrer"><code>dotenv</code></a> 和 <a href="https://github.com/motdotla/dotenv-expand/blob/master/CHANGELOG.md" target="_blank" rel="noreferrer"><code>dotenv-expand</code> 更新日志</a>。</p><h2 id="advanced" tabindex="-1">进阶 <a class="header-anchor" href="#advanced" aria-label="Permalink to “进阶”">​</a></h2><p>下列改动仅会影响到插件/工具的作者：</p><ul><li><a href="https://github.com/vitejs/vite/issues/11036" target="_blank" rel="noreferrer">[#11036] feat(client)!: remove never implemented hot.decline</a><ul><li>使用 <code>hot.invalidate</code> 来代替</li></ul></li><li><a href="https://github.com/vitejs/vite/issues/9669" target="_blank" rel="noreferrer">[#9669] feat: align object interface for <code>transformIndexHtml</code> hook</a><ul><li>使用 <code>order</code> 来代替 <code>enforce</code></li></ul></li></ul><p>此外，还有其他一些只影响少数用户的破坏性变化。</p><ul><li><a href="https://github.com/vitejs/vite/pull/11101" target="_blank" rel="noreferrer">[#11101] feat(ssr)!: remove dedupe and mode support for CJS</a><ul><li>您应该迁移到 SSR 的默认 ESM 模式，CJS SSR 支持可能会在下一个 Vite 主要版本删除。</li></ul></li><li><a href="https://github.com/vitejs/vite/pull/10475" target="_blank" rel="noreferrer">[#10475] feat: handle static assets in case-sensitive manner</a><ul><li>您的项目不应该依赖于会被不同操作系统忽略大小写的文件名。</li></ul></li><li><a href="https://github.com/vitejs/vite/pull/10996" target="_blank" rel="noreferrer">[#10996] fix!: make <code>NODE_ENV</code> more predictable</a><ul><li>有关此更改的解释，请参阅 PR。</li></ul></li><li><a href="https://github.com/vitejs/vite/pull/10903" target="_blank" rel="noreferrer">[#10903] refactor(types)!: remove facade type files</a></li></ul><h2 id="migration-from-v2" tabindex="-1">从 v2 迁移 <a class="header-anchor" href="#migration-from-v2" aria-label="Permalink to “从 v2 迁移”">​</a></h2><p>请先查看之前 Vite v3 文档中的 <a href="/guide/migration-from-v2">从 v2 迁移指南</a> 了解迁移到 v3 所需要的更改，然后再继续执行本页提到的相关更改。</p>',29)])])}const u=a(i,[["render",l]]);export{f as __pageData,u as default};
